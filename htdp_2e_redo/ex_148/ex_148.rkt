#|
Author: Mark Beltran
Date: August 2, 2023

Exercise 148. Compare the function definitions from this section (sum,
how-many, all-true, one-true) with the corresponding function definitions
from the preceding sections. Is it better to work with data definitions that
accommodate empty lists as opposed to definitions for nonempty lists?
Why? Why not?
|#

; I'm not entirely sure but I think that using non-empty lists is
; redundant. It seems better to use data definitions that accomodate empty
; lists.
