#|
Author: Mark Beltran
Date: June 10, 2023

Adds a natural number to pi without using the + primitive operator.
|#

; DATA DEFINITION //////////////////////////////////////////////////////////////

; a nat_num is one of:
; - 0
; (add1 nat_num)
; Interp.: a natural/counting number
; (define (nat1 0))
; (define (nat2 5000))

; FUNCTION DEFINITION //////////////////////////////////////////////////////////
